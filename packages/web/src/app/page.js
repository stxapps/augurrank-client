import { LandingPre } from '@/components/LandingPre';

export const metadata = {
  title: 'AugurRank - Your visionary on chain',
  description: 'Challenge yourself to envision the future, keep score on the chain, and brag about it forever.',
};

export default function Page() {
  return <LandingPre />;
}
