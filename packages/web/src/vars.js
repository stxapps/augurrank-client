const btcPrice = {
  didFetch: false,
  timeId: null,
};

const burnHeight = {
  didFetch: false,
  timeId: null,
};

const gameBtc = {
  didFetch: false,
};

const me = {
  didFetch: false,
};

const agreeTerms = {
  pred: null,
};

const pendingPreds = {
  isRunning: false,
  timeId: null,
};

const vars = { btcPrice, burnHeight, gameBtc, me, agreeTerms, pendingPreds };

export default vars;
